import type { TConfig } from '@lumina/core'

export const config: TConfig = {
  name: 'Breadcrumb',
  type: 'breadcrumb',
  editor: {
    children: false,
    editable: true,
    delete: true,
  },
}
